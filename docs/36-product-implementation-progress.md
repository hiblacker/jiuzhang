# 通用数仓产品实施进度

范围：30 号产品报告及 31/33 号验收；复核缺陷修复记录见 [35](35-lake-review-and-remediation.md)。用户要求持续本地提交，页面先简单接通真实操作，后续统一框架。以下只记录实际实现，未验收项继续开发。

## 持续入湖控制面

V012 新增不可变计划版本、应有日期窗口和执行尝试。一个来源只有一个活动计划；修改使用预期版本比较，保留旧定义。每次重试保留旧尝试并获得新租约令牌。

| API | 用途 |
|---|---|
| `GET/POST /api/v1/lake/plans` | 查看/保存计划新版本，`runtimeRef` 只指向 Worker 管理员配置的执行资源 |
| `POST /plans/{id}/state` | `ACTIVE` / `PAUSED` |
| `POST /plans/{id}/trigger` | 指定日期及原因触发；显式 revision 产生修订，普通重复请求返回同一窗口 |
| `POST /calendar/reconcile` | 有界登记漏跑日历，每计划每次最多补齐 31 个窗口；不能恢复的历史快照标 `MISSING` |
| `GET /windows`、`GET /executions` | 查看日期及尝试，支持 `planId` 过滤 |
| `POST /executions/claim` | Worker 按已配置 `runtimeRefs` 领取任务；同计划互斥 |
| `POST /executions/{id}/heartbeat`、`/finish` | 以 owner、租约令牌和有效期围住执行；完成请求可安全重放 |
| `POST /executions/{id}/retry`、`/cancel` | 管理员重试/取消；旧租约拒绝提交 |

表中缩写路径均相对 `/api/v1/lake`。计划、触发和管理操作用 Admin 身份；领取、心跳和完成用 Worker 身份。计划配置不接收路径、SQL、shell 或凭证；资源路径只在 Worker 本地注册表配置。调度来源的 MySQL manifest 必须经执行完成事务登记，不能绕过租约直接提交。

日历驱动通过 `LAKE_CALENDAR_DRIVER` 二选一：默认 `external` 由外部已选调度组件调用 reconcile；本地运行可设置 `local`，后台每 10 秒对账，不依赖页面。此处是平台交付日历，DolphinScheduler 正式集成与部署仍需后续验收。

本批真实 PostgreSQL/HTTP 测试覆盖：先手动触发今天仍可发现昨天的遗漏、重复对账不重复入队、并发领取只有一个成功、租约过期后自动排队新尝试、旧租约提交拒绝、取消可见、重试请求幂等。该控制面切片提交为 `632c308`。

## 独立执行与简单页面

`apps/ingestion-worker/lake-runtime.mjs` 是持续入湖执行进程；复用已有 Node 适配器，原 Java 合成 Worker 保留用于兼容验证。它从控制 API 领取已批准的执行配置，后台续租，取消或失联时终止适配器进程组。完成回执先写本地 outbox 再提交，响应丢失时只重放回执；过期租约不能误报成功。路径与凭证配置在管理员本地注册表，不从任务 HTTP 请求传入。

配置模板见 [Worker 注册表](templates/lake-runtime.example.json)，只保留实际启用的配置，替换绝对路径及来源编码。`datePartitioned: true` 时文件目录为 `<inboxRoot>/<YYYY-MM-DD>/`，保持每日日份独立；CSV 等仍以 `.done` 标记闭合。

```bash
# CONTROL_API_WORKER_TOKEN 从运行环境注入
node apps/ingestion-worker/lake-runtime.mjs --registry /path/to/private-runtime.json \
  --control-api http://127.0.0.1:8080 --instance lake-worker
```

真实集成验证已从 HTTP 入队开始启动独立 Node Worker，完成合成目录的原件封存、CSV 解析、数据库完成状态及回执确认；前导零 `001` 保留。另有网络中断测试验证完成响应丢失后不重新读取已变化的外部文件。

简单控制台已接通来源、计划、暂停/恢复、触发、取消、重试和日历查询。使用本机已有 Playwright 1.62.1 实际点击以上操作通过；无页面脚本错误，390px 视口无页面横向溢出。此测试属于开发验证工具，未新增应用依赖。项目角色、资产及建模页面随后追加。

## 真实 MySQL 持续执行验证

2026-09-16 已通过计划入队、独立 Worker、周期心跳、原件封存、控制面事务提交运行真实指定库。执行 `10`、批次 `exec-10`、数据库系统批次 `14`：157 张表、1,617,565 行全部提交，原始字节数 639,135,712；编码为修复后的 `mysql-char-v2`，不与旧双重转义原件的字节数混算。旧全量和每日快照均保留。

`tools/lake-discover.mjs` 使用加密只读连接发现基础表、列类型与有序主键；Worker 在快照前后各核对批准清单。新增、删除、类型/主键变化产生私有待审核发现记录并阻断成功提交，不能自动改正式 Schema。元数据估计行数变化不算结构变化。本次前后均为 157 张表且结构一致。此路径沿用已授权测试源 TLS 例外，通用默认仍是严格校验。

2026-09-15 的实际每日快照与 09-16 的执行均有真实原件，但当前不能据此宣称长期跨日稳定性。自动日历、重启恢复与部署恢复还需联合验收，Schema 变更审批页面随后接通。

## 每日文件交付契约

Worker 注册表的 FILE_SCAN 配置可指定绝对路径 `deliveryContract`，参考[交付契约](templates/file-delivery.example.json)。`DAILY_SET` 检查固定文件集；`MANIFEST` 使用 `_delivery.json` 加 `.done` 封闭包，包含日期、生产方递增 revision 和 `files: [{path, logicalId, sha256}]`；`EVENT` 接收已就绪文件并报告尚未就绪数量。无须绑定传输工具。扫描有包含/排除、深度、积压、单文件及行数限额。

默认 `.done` 判定就绪；显式 `STABLE` 需要至少两次扫描且大小/修改时间稳定达到配置时长，标记为启发式证据。每日缺交、部分未齐、迟到、合法空包、重复和更正分开记录。空包必须有清单 `empty: true` 且规则批准；目录断开不能当空交付。清单原文按哈希保留，文件复制前后校验哈希。逻辑 ID 允许清单文件改名后去重；内容修订及解析配置版本均保留历史。CSV 支持编码、分隔符和显式空值；Excel 支持工作表/范围，缺缓存的公式拒绝解析；JSON 支持记录路径、必需字段；Parquet 保留精确数值并允许显式无时区时间配置。

本地行为验证：多文件未齐、缺失与迟到、重复行数、修订审核、稳定观察、改名、原始清单保留、合法空交付、目录断开、事件半写和规则重处理，以及四格式解析/Worker 回执，共 17 项通过。未宣称真实业务文件已对接；原件独立重处理入口和资产登记继续完善。

## 项目权限与统一资产目录

V013 新增项目、服务/成员身份、项目角色、来源归属和文件/API 资产版本。身份令牌仅创建时返回，数据库仅存 SHA-256；每个请求验证启用状态。`OWNER` 管理项目成员，`ENGINEER` 用于数据开发，`VIEWER` 查看已授权项目；平台管理员负责创建项目/身份、分配来源和撤销身份。来源不能通过重复绑定偷偷迁移到其他项目。查询均在服务端加入项目范围，原件路径和凭证不返回给项目成员。

`/api/v1/warehouse/projects/{project}/assets` 将 MySQL 对象版本与目录/API 原件统一显示；资产详情给出字段、来源、契约版本、原始/解析状态及行数。目录/API 字段目前来自最多 25 行观察并明确标为 `OBSERVED_SAMPLE`，不冒充完整声明 Schema。资产记录和执行完成由有效租约保护，在同一数据库事务写入；重复回执不重复创建资产。解析失败时已封存原件仍可登记为失败或仅接收状态。

`/coverage/{run}` 以该运行绑定的固定清单计算分母及成功、失败、缺失。真实 MySQL 项目已绑定完成，系统批次 14 通过接口查询为：必需 157、提交 157、失败 0、缺失 0、1,617,565 行。保留所有旧资产版本，可分页查看。

简单页面现支持创建项目/身份、分配来源、设置角色、切换项目成员视图、资产分页、字段与来源详情及批次覆盖。Playwright 实际操作 15 项通过，其中包括页面触发 → 独立 Worker 接收合成 CSV → 页面查看字段；无脚本错误，移动视口无页面溢出。真实 API/数据库测试新增跨项目拒绝、禁止自提权、禁止读取平台接口、立即撤销、资产与完成事务一致性。该轮 Node 98 项全部通过；控制 API 共 55 项，54 通过、1 个可选历史原件登记测试跳过。

V013 已在隔离本机 PostgreSQL 执行，后续只追加迁移。恢复需要联合保留原始区、文件/API 账本、PostgreSQL 及模型版本；尚未完成联合还原演练，不能据此宣称部署验收。

## Git SQL、dbt 与发布查询

V014/V015 增加模型版本、独立构建队列、候选 Schema 冻结、数据集发布与行列策略。模型注册只接收 Git 提交、完整包摘要、来源绑定和数据契约；独立 Python Worker 从受控 Git 读取实际 SQL，复核输入哈希后装载声明类型的 PostgreSQL 表，再执行真实 `dbt build`。依赖与配置见[运行说明](../apps/model-worker/README.md)。不是之前 JSON 投影脚本的改名。

已实测 Python 3.12.14、dbt Core 1.11.15、postgres adapter 1.11.0；54 个包在安装前记录版本、许可及 wheel 哈希，全部从国内源安装。旧 1.12.4 PoC 锁保留；其依赖的下载式实验解析器需要额外 GitHub 二进制，本轮选择可从国内镜像完整安装的 Python v1 组合。`pip check` 通过。

真实 PostgreSQL/HTTP/dbt 联合测试通过两套合成主题。验证了：字段按声明类型进入 SQL 表、唯一键和非空门禁、执行角色无法改写冻结表、精确小数与前导零、项目隔离、VIEWER 行列策略、固定 release 查询/CSV 导出、两个候选只有预期活动版本匹配者可发布、较旧输入拒绝晋级、质量失败保留旧发布。dbt 节点状态、内部模型依赖与输入资产时间随构建保留。API 全套 56 项中 55 通过，1 项可选历史原件登记测试跳过。

简单页面新增模型登记、候选构建、质量/依赖详情、变更契约差异、发布、历史、授权查询与导出。模型 SQL 在 Git 维护；当前采用全量从固定输入重建，不声明任意增量模型已支持。版本快照模型保留源状态/删除标志，未为真实源编造业务映射。模型工作进程、API 完成与原子发布分别分离；候选数据当前与元数据位于同一 PostgreSQL 数据库的不同 Schema/角色，物理分库仍待部署切分。

真实 MySQL SQL 验证也已完成：项目 4、数据集 9、构建 14、release 6 从系统批次 14 的 VERSION 原件构建。295 行、7 个字段通过原件逐单元格投影比较；包括源状态及删除标志原样保留。仅使用已封存输入，未连接或修改源库。页面 19 项操作（含模型版本、发布历史、查询和 CSV 下载）实测通过。

校正 `118b883` 提交说明：其“Node 98 项通过”误沿用了前次结果，提交前最后一次实际为 97 通过、1 个部署断言仍写死 dev.9。随后将测试改为验证明确版本号且与部署说明一致，再次完整运行得到 98/98；该校正随下一提交保留。SQL/API 和页面结果未受此测试影响。

## 原件重处理与 API 接收边界

V016 为执行窗口记录封存输入引用。管理员可在执行详情点击“重解析原件”，或调用 `POST /api/v1/lake/executions/{id}/reprocess`。同一父执行已有待执行重处理时返回同一窗口；实际解析使用当前批准的运行配置并保留新旧批次/解析摘要。目录与 API 分别由 `file-reprocess.mjs` 和 `api-reprocess.mjs` 读取封存输入，继续通过租约、outbox 和资产登记事务完成，不依赖外部文件继续存在或接口继续在线。

实际测试已删除合成目录后，从真实 HTTP 队列完成文件重解析；API 模拟来源总调用数为 1，首次结构规则错误保留原响应，关闭模拟服务后修正规则，重处理成功并登记 PARSED 资产。多页只收到部分页面时仍标 INCOMPLETE；未封存包成员、未批准修订不能通过重处理冒充完整交付。模型只接受完成交付中的已解析资产。API 增加每秒请求上限（默认 5），429/临时故障重试遵守该上限，接收原响应后立即写独立检查点。

本切片完整验证：Node 103/103；控制 API 57 项中 56 通过、1 项可选历史登记跳过；真实浏览器 20 项操作通过、无脚本错误。此处证明原件可重新解析，不宣称业务 API 已接入；分页过程中进程终止后的自动续传与联合恢复继续收尾。来源范围、真实源测试和系统部署仍按前文边界分别报告。

## 每日等待与任务恢复

文件未齐、等待完成标志、目录暂不可用和逾期未交会保留每次观察，按计划契约 `pollSeconds`（默认 300，范围 60–3600）自动复查；`lateDays`（默认 7，范围 0–31）限制等待期限。等待不消耗故障重试额度，解析错误与审核项不自动放行。瞬时网络错误和租约过期按失败预算延迟重试；运行总超时即使仍有心跳也会被回收。计划升版会取消旧版本排队任务；跨过日期的当前态采集明确标记无法恢复历史。

真实 HTTP/PostgreSQL 测试完成三次文件等待和再调度、失败预算、计划升版取消与旧重试拒绝。完整 API 58 项中 57 通过、1 项可选历史登记跳过；包括真实 dbt 与文件/API Worker 链路。

## 本机启动与联合恢复

新增[本机运行手册](../deploy/LOCAL_RUNTIME.md)及受控进程管理工具：凭证通过私有 JSON 引用注入，API/页面探活，按记录的 PID 和启动身份停止服务。已实际完成整套原生栈停止、启动和备份后自动恢复。

联合冷备份保存 PostgreSQL、湖区账本/原件/模型工作区和 Git bundle，逐文件校验后封存。本次 3,520 个文件、4,541,665,573 字节；恢复到独立目录和 PostgreSQL 17 端口。16 项迁移与校验和一致，真实系统 157 张表、1,617,565 行登记一致；release 6 的 295 行 SQL 内容摘要与原库相同。恢复后的独立 API 对资产覆盖、数据集、发布历史及 295 行查询逐项比较通过，无认证请求返回 401。原栈已恢复运行，恢复测试库随后停止保留。

工具行为测试通过缺失、额外文件、篡改、符号链接拒绝、权限与禁止覆盖；不自动删除失败备份、原件或测试还原目录。此次为本机相同主版本物理恢复，不代表跨 PostgreSQL 主版本升级、异机 NAS 或生产恢复验收。Compose 的实际 Worker 包装仍需与本机已验证链路对齐。

## 剩余出口条件

1. Worker 用同一执行契约运行真实 MySQL/目录/API；原件和完成回执可恢复，结构变化有发现/阻断/确认路径。
2. 文件每日交付：包判齐、空交付、缺失、迟到、修订、解析规则版本及原件重处理。
3. 项目/角色、主题、资产详情与受控预览；简单页面提供真实操作。
4. Git SQL/dbt 模型、依赖与变更影响、质量、版本发布、服务端行列授权查询；第二主题用配置复用。
5. 本地启动、原始区/元数据/模型联合备份还原、前向迁移及资源结果；真实跨日及 NAS/生产状态分别说明。

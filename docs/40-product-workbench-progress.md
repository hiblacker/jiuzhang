# 产品工作台实施与验证

状态：A–E 产品能力及 F 本地自动验收已交付；人工试用与真实连续运行待验收。执行依据为 [38 号方案](38-next-product-implementation-plan.md)和 [39 号验收契约](39-next-product-acceptance-contract.md)。2026-09-16 确认 PR #1 已合并，开发基于 `11322a0`；不改写一期验收记录。

## 工作包

| 工作包 | 当前状态 | 验证范围 |
|---|---|---|
| A 工作台基础 | 已实现 | Vue/TypeScript/Naive UI 同源工作台；邀请、登录、CSRF、撤销、项目切换、设置及合成 CI；真实浏览器通过 |
| B1 系统目录 | 已实现 | 系统、环境实例、责任人、并发配额、共享目录范围；权限、过期版本和分页检查通过 |
| B2 连接与接入向导 | 已实现 | 批准资源、连接/通道版本、异步预检、MySQL 表清单与例外、四格式/REST、复制新通道；浏览器和实际 Worker 通过 |
| B3 多系统调度运营 | 已实现 | 共享配额/速率、来源账本、影响预览、系统/实例/连接/通道操作、每日到齐与退役；合成并发/隔离/幂等通过 |
| C 模型与资产 | 已实现 | 分页选择资产、Git 封存模型包、绑定构建、8 类质量规则、dbt 节点依赖；真实 SQL 与浏览器通过 |
| D 自动交付 | 已实现 | 固定完整输入、按日刷新、批准自动发布、迟到审批、失败/取消的幂等重试；多对象/外部迟到、权限撤销和暂停恢复通过 |
| E 消费与运营 | 已实现 | 固定发布查询/导出、类型筛选/排序、多行条件授权、新鲜度、审计、异常真实恢复、服务令牌；真实 HTTP 与浏览器通过 |
| F 集成与试运行 | 本地自动验收通过 | 全新容器、同源页面、一期备份隔离升级、157 表来源补录、9 个旧发布查询、1 万对象分页通过；人工与长期运行见下方边界 |

## 第一批验证（2026-09-16）

- 独立本机 PostgreSQL 17.6 集群，仅使用合成测试源；未迁移正在运行的一期数据目录。
- API：61 tests、0 failures、0 errors、1 skipped。历史 manifest 文件条件测试跳过；本次包含真实数据库角色、迁移、文件/API Worker、dbt 模型回归及新增账号/系统目录场景。
- `npm run build --prefix apps/console`：TypeScript 检查和 Vite 构建通过。主包大小提示保留为后续模块拆分依据。
- npm audit：116 个解析依赖，报告 0 个已知漏洞；不等同于应用安全审计。依赖下载使用国内源；漏洞查询使用 npm 官方只读审计接口。
- 依赖固定版本见 [实施锁](dependencies/product-workbench.json)，直接/传递包及完整性记录见 [前端清单](dependencies/console-transitive-lock.json)。Spring Security 6.5.11 由既有 Spring Boot 3.5.16 BOM 固定。

## 迁移与恢复

新增 V018 账号/邀请表、V019 系统目录及项目关联；V001–V017 不变。现有 source、批次、模型、发布及原件身份保持。迁移由管理员显式执行，应用启动不自动修改 Schema。

浏览器身份与既有服务令牌分开；开户邀请只回显一次，24 小时内有效，数据库存摘要。负责人只能邀请新账号或管理本项目成员；已有账号密码重置由平台管理员执行。会话撤销/密码重置递增凭证版本，下次请求立即拒绝旧会话。重置后旧密码不能继续登录。

前端开发用 `npm run dev --prefix apps/console`，API 代理默认指向本机 60185；打包前运行前端构建，Maven 将 dist 纳入同源静态资源。一期页面保留为 [legacy.html](../apps/console/legacy.html)。整栈安装/切换脚本在 F 完成前仍属待升级，不声明当前运行服务已切换。

回退应用时保留新增表与迁移账本；不执行 DROP、不复用编码、不删原件。确认备份后向前修复配置及关联，再重启对应版本。未进行人工同行评审或生产部署。

## 统一接入验证（2026-09-16）

- V020 增加执行环境、已批准资源、连接/通道版本和有租约的异步发现队列。Worker v2 注册表一次登记本机资源边界，按控制面返回的配置文本及 SHA-256 获取新通道；v1 profile 兼容保留。
- API 回归 62 项：61 通过、1 项历史条件跳过。新增端到端测试实际执行 CSV、xlsx、JSON、Parquet 与模拟 REST 的发现、计划启用、运行和资产检查；相同连接可继续建立独立通道。
- Node 相关检查 10 项通过，覆盖旧 outbox 重送、REST 分页/错误/精度、结构差异，以及新配置摘要、资源边界和符号链接越界拒绝。
- 已授权 MySQL 测试源通过新 Worker 配置解析机制发现 157 张基础表、0 个不支持对象；只读元数据，无业务行抽取。测试 TLS 例外仍校验此前授权文件绑定的配置摘要。
- 本机文件解析复用已有 openpyxl 3.1.5、PyArrow 18.1.0；首次新增四格式测试因误用了不含文件依赖的 dbt Python 而失败，切换到已安装的文件解析 Python 后通过，未新增安装。
- 浏览器管理员的审计保留具体账号；后台再次检查账号启用状态及权限。资源引用不包含宿主绝对路径或凭证值。当前文件/API 计划显式限定 Asia/Shanghai，其他时区拒绝启用，不冒充已支持。
- 连接与通道版本接口已具备；连接共享变更影响、系统运行汇总、生命周期及多系统资源调度属于 B3，仍待完成。

## 多系统调度与交付（2026-09-16）

- V021 增加系统/环境/资源配额、调度轮转记录、批量操作账本和不可变交付约定。领取只串行化短事务，实际采集由不同 Worker 并发执行；同物理资源别名使用管理员登记的共享组限额。
- 三类采集器使用按来源互斥和账本。首次读取兼容旧共享账本，仅复制匹配来源的条目；不覆盖旧账本。切换版本前必须停止旧采集进程，不能让旧二进制和新二进制同时写同一湖目录。
- 预检与日常执行轮换，预检回执也写持久 outbox；共享 REST 速率等待后再次检查租约，失效或取消的尝试不继续读取源。
- 真实 HTTP/JDBC 集成验证：同连接双通道领取只有一个获得资源，同时另一类资源可执行；完成后剩余通道可领取，排队没有生成失败尝试。每日到齐固定必需通道分母；新约定不覆盖已冻结窗口。
- 批量预览固定对象及版本；每项成功独立提交，断线重放复用结果。无权或版本过期逐项返回错误。暂停阻止后代新增调度/领取，退役先检查在途与下游引用，保留资产和历史。页面提供操作预览和每日交付约定。
- 连接器目录只准入当前三类实现，模板只含非敏感声明。非日频日历通过 daysOfWeek 明确指定；非应交日不生成定时采集窗口。

## 资产与模型工作台（2026-09-16）

- V022 增加批准 Git 仓库、项目授权、异步打包请求、不可变模型包和构建发起身份；补充授权字段类型基线。旧模型 profile 协议保留。
- 新 [Worker v2 示例](templates/model-runtime-v2.example.json)登记批准仓库及项目范围。仓库在持久目录；页面选择路径和分支/标签后，由 Worker 固定提交和内容摘要，封存包独立于镜像与原 Git 目录。新增模型无需重建应用镜像。模型包目录也必须纳入备份。
- 真实 dbt 集成实际验证：未提交 SQL 不执行；临时移走 Git 目录后封存包仍可构建；8 类声明式规则全部运行，修改范围阈值后拒绝候选并保留旧发布。
- 规则覆盖类型、非空、唯一、枚举、数值范围、与固定输入的引用完整性、行数变化和新鲜度。无基线/零基线单独表达，规则结果保存版本、阈值、实测值及阻断等级；不回显原始问题行。规则通过 Git 契约演进，页面查看结果。
- 构建默认要求同一来源的多个对象来自同一完整批次。来源暂停、身份撤销或资源不再授权时，新构建领取受阻。发布再次检查来源、模型版本、当前 release 和授权字段类型兼容性。
- API 回归 62 项：61 通过、1 项历史条件跳过；新增模型包和规则场景纳入原真实 dbt 集成方法。Vue 类型检查/构建通过，Python 编译通过。

## 每日自动刷新（2026-09-16）

- V023 保存版本化刷新计划、业务窗口和固定输入集合与构建关联。日历只协调元数据，不在 HTTP 或调度线程执行抽取/SQL。每个窗口独立事务；失败不会回滚其他数据集。
- 同来源对象选择同一 COMPLETE 交付批次；最新修订未到齐时等待，不退回旧完整修订冒充更新。记录输入业务日期、来源时区、实际接收时间、执行批次及修订；跨源时差超过约定需处理。
- 默认人工发布；自动发布要求固定模型版本已有人工发布，且独立项目服务身份拥有相应权限。模型改变、当前 release 竞争、较旧输入、来源暂停或身份撤权均执行发布检查。
- 真实 HTTP/文件/dbt 集成通过重复对账只建一次、更正形成新输入集合、人工首发、批准自动发布、撤销服务权限阻断、暂停阻断发布和恢复继续推进。Vue 刷新计划表单、逐窗口输入/缺项与操作记录已接入。
- 自动日历默认回看近 31 个业务日的更正，额外推进历史未检查日期；更早的修订可指定业务日手动对账。计划保存固定采集计划版本，采集定义更换后需要审阅并更新刷新计划，不能静默替换输入来源。

## 数据服务与日常运营（2026-09-16）

- V024 保存查询审计、异常及观察记录和 Worker 存储心跳；V025 增加项目服务身份归属与令牌版本。浏览器邀请/会话与服务令牌分别管理。
- 查询支持字段类型匹配的等值、集合、范围和最多 5 个排序字段，按唯一键补全稳定排序。release 显式固定；数值以 SQL 文本返回，分页明确 hasMore，CSV 只导出当前有界页。
- 查询/导出审计保留身份、release、权限版本、请求 ID、字段、条件摘要、行数、耗时与结果；不保存条件原值或结果行。失败审计独立事务保存。
- 数据集同时展示是否有发布、新鲜度和本轮刷新状态。扩展规则未配置会如实标记；旧发布不会因新一轮失败消失。工作台显示当前项目来源/数据集数量、部分数据集新鲜度和待处理异常。
- 异常按逻辑窗口/类别归并，重复观察不新增事项；确认只分配责任及记录处理，恢复必须关联完整交付或成功发布。运行页面支持详情、重试、取消与受控结构变更批准。
- 项目负责人可创建独立服务身份、按预期版本轮换/停用令牌；只回显一次秘密，不写浏览器持久存储。管理员设置页面提供执行资源、批准模型仓库及项目初始化。
- 本次 API 62 项：61 通过、1 项历史条件跳过。新增真实 HTTP 验证精确数值范围与集合、隐藏列排序拒绝、过大分页拒绝、审计脱敏、令牌立即失效、异常幂等/确认/恢复。Vue 类型检查与构建通过。

## 本批完整验证与交付（2026-09-16）

- 可重复入口 [verify-product.py](../tools/verify-product.py) 实际通过：前端类型/构建、109 项 Node、3 项 Python、64 项 Java/API（63 通过、1 项历史 manifest 条件跳过）、新版 Playwright 浏览器、规模、文档和 diff 检查。Maven 实际执行 `verify`，不以编译通过代替业务验收。
- [浏览器测试](../tests/product-browser.mjs)实际从邀请开户、登录、登记系统/实例、配置文件接入、预检/启用/采集，到 Git 打包、选择资产、dbt 构建、发布、页面邀请查看者、多行条件授权、精确查询/CSV、跨项目拒绝和会话撤销。0 页面脚本错误、0 浏览器持久凭证；已检查生成的资产、模型和查看者截图。
- 实际修复浏览器账号切换保留旧项目、行权限多条件丢失、固定旧 release 未同步字段说明、实例操作入口、连接变更影响展示、失败刷新缺少安全重试，以及旧 profile 映射后漏用共享 API 速率等问题。重试新增 V026，只追加构建历史，不改写旧失败回执或模型包。
- 完整输入集测试实际完成：同来源订单和客户对象、另一来源依赖分批到达；缺项时没有构建；到齐后仍需批准人工迟到策略；批准后固定 3 个输入并执行 dbt；同源两个对象的 executionId 相同。不同业务日偏移被拒绝。
- 规模样例 50 个系统、150 个连接、300 个通道、10,000 个资产。系统 1 请求 9 ms、连接 1 请求 10 ms、通道 2 请求 39 ms、资产 50 请求 1,991 ms；全量遍历无重无漏，筛选/最后一页正确，一个系统的影响为 6 个可见通道。该结果仅为本机合成元数据分页观察，环境 Java 21.0.5、10 个逻辑处理器、JVM 最大堆 6 GiB，不是采集吞吐承诺。
- 一期 4,541,665,573 字节冷备份经校验后恢复到独立目录，使用 PostgreSQL 17.6。备份含 V001–V016，逐项确认 Git 基线 V001–V017 未改，追加到 V026。真实 157 表来源通过 existingSourceId 补录系统/实例/连接，原来源、计划、批次、资产、原件引用及模型/release 指纹全部保持；9 个发布、308 行与升级前冻结表一致，其中包含真实 VERSION 发布。升级后的同源工作台可打开；未联系业务源、未迁移原环境。见 [升级测试](../tests/product-upgrade-integration.py)。
- 新容器安装实际完成 V001–V026、数据库角色、Java 同源页面、空闲 Worker v2 和一次性管理员邀请。兼容 v1 的容器文件→dbt→发布→查看者查询与 Worker 重启检查通过。构建使用国内源和 57 个固定 Linux wheel；在未登记执行环境时 Worker 不获授权，容器验收现在先登记批准环境。
- 前端 119 个解析依赖都有许可声明，npm 官方只读审计报告 0 已知漏洞。[后端清单](dependencies/backend-runtime-lock.json)记录实际包内 45 个运行 JAR、摘要和 POM/包内许可证据；新增 Spring Security 为 Apache-2.0。已有 Logback 等多许可声明如实保留，本清单不替代组织对外分发审批。
- [GitHub 工作流](../.github/workflows/product.yml)已落地，Action 固定提交；Node/Java/Python/PostgreSQL/Maven 固定版本，Maven 国内下载与官方 SHA-512 一致，Playwright Chromium 国内下载地址返回 200。本地执行同一验证入口通过；尚未推送，未声称远程 CI 运行。
- [41 号手册](41-product-workbench-runbook.md)覆盖开户、管理员资源准备、三类接入、Git 模型、自动刷新、授权消费、运维、升级和恢复。旧页面源码与一期证据保留，部署配置改用新同源工作台。未进行人工同行评审或生产部署。

## 30 项契约的证据映射

下表区分实现/自动验证与现场验收，避免把一次合成运行写成长期可用。

| 项 | 已实现及本次可复查证据 | 现场条件 |
|---|---|---|
| A01 | 真实会话/CSRF/角色/项目隔离集成与浏览器登录、邀请、退出、撤销 | 待实际成员试用 |
| A02 | 同一批准目录资源通过页面建立来源，Worker 采集并查资产 | 用户日常目录按约定投递 |
| A03 | 授权 MySQL v2 元数据发现 157 表；整库表清单、例外与范围确认入口 | 真实每日运行沿用既有源授权 |
| A04 | CSV/xlsx/JSON/Parquet、模拟 REST 实际 Worker；精度/分页/越界与错误测试 | 真实 API 合约待提供 |
| A05 | 交付缺失/半写/空包/迟到/断连/修订的文件测试 | 真实落目录稳定观察 |
| A06 | 浏览器选 Git 包/资产并真实 dbt；封存包脱离原 Git 后仍执行 | 真实模型由业务确认 |
| A07 | 同源多对象与外部依赖迟到，缺项不构建，到齐固定批次；源批次一致性检查 | 真实多系统业务日期映射确认 |
| A08 | 回执重放、租约过期、重启恢复及刷新集合幂等 | 长期进程观察 |
| A09 | 更正产生新集合/构建；固定模型，变更必须更新计划；旧发布保留 | 业务模型变更审批 |
| A10 | 8 类版本化质量规则真实 SQL 测量、阻断、旧版可查 | 业务阈值和质量责任人确认 |
| A11 | 人工首发、自动策略、竞争/旧输入/撤权/暂停的发布门禁 | 真实自动发布策略批准 |
| A12 | 查看者行列授权、跨项目/隐藏字段拒绝、CSV 与脱敏审计 | 实际成员分配 |
| A13 | 固定 release、精确值、排序分页和限额拒绝；旧 release 字段说明 | 大结果下载采用有界分页 |
| A14 | 1 万对象全分页、筛选、空结果与异常展示；浏览器实操 | — |
| A15 | 站内异常归并/确认/成功后恢复、新鲜度与失败保旧测试 | 连续每日观察 |
| A16 | 一期备份隔离升级、旧文件校验、157 表及 9 个 release 查询 | 原环境升级另按手册执行 |
| A17 | 全部合成入口本地通过；锁定依赖的 CI 配置完成 | GitHub 远程运行待推送 |
| A18 | 三条主流程手册和浏览器自动化已准备 | **待未参与开发的使用者实操** |
| A19 | 系统/实例下三类资源统一建模；文件/API 共用 Worker 动态新增与复用连接 | 三类真实来源同系统连续运行 |
| A20 | 稳定 source 身份、来源账本/锁及原件路径隔离，目录权限独立 | 实际测试/生产资源分别授权 |
| A21 | 不可变连接/通道版本、显式切换、影响预览、旧任务配置和回执保留 | 真实源凭证轮换由管理员执行 |
| A22 | 共享目录不扩权、隐藏实例计数、连接/通道/汇总权限及异常名称范围 | — |
| A23 | 冻结每日约定、必需/可选通道、历史分母、无计划与部分范围状态 | 实际交付约定确认 |
| A24 | 通道变更差异、MySQL Schema 审批、影响、理由及新来源语义检查 | 真实新增对象确认 |
| A25 | 原子租约配额、连接等待、独立来源并发、共享速率与失联回收测试 | 真实负载与峰值容量验证 |
| A26 | 冻结目标预览、逐项独立结果、部分失败/版本冲突、重放幂等 | — |
| A27 | 层级暂停阻止新领取/发布、退役检查在途/依赖并保留历史 | — |
| A28 | 准入三类连接器、安全声明模板、页面复制新通道、共享资源组预算 | 新连接器须另行准入 |
| A29 | 实际 157 表旧来源补录，所有原身份/哈希与发布查询不变 | 原环境补录按手册执行 |
| A30 | 50/150/300/10,000 元数据规模、完整分页和影响数量记录 | 不能视作吞吐压测 |

真实连续 7 个每日窗口、真实 API/目录运营、A18 人工试用及 NAS/生产上线尚未验收。这些状态不会通过合成回放自动改成通过。本地开发交付可据此进入业务试用，现场反馈继续按独立问题修复。

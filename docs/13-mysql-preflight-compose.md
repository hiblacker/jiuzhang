# 13 MySQL连接预检与Compose双版本基线

- 日期：2026-09-10。
- 状态：本文第2节保留初次无认证预检历史；后续用户已授权现有测试账号、本地Docker已可用、NAS已选定Compose v2.40.3。真实连接遇到TLS证书信任失败，详见[最新执行记录](14-mysql-test-discovery.md)；尚无成功登录、SQL执行、结构发现或NAS部署。
- 关联：[只读数据库交接](12-source-db-discovery.md)、[PoC输入](11-poc-confirmed-inputs.md)。

## 1. 用户本轮确认

- 数据库为MySQL 8.1.0；连接文件已填写；本机可访问，无VPN。
- 元数据调查允许覆盖所有库，以账号实际可见范围为上限；这不等于授予写权限或读取所有业务行。
- NAS上存在两个Compose入口，版本如下（用户提供的命令输出，不是本机或NAS远程实测）：

| 入口 | 版本 | 执行身份/边界 |
|---|---|---|
| PATH中的docker-compose | v2.20.1-6047-g6817716 | 用户贴出的普通用户执行结果；实际绝对路径待核查 |
| /root/.docker/cli-plugins/docker-compose | v2.40.3 | 用户贴出的root执行结果；不能假定普通部署账号可访问 |
| docker Engine CLI版本输出 | 24.0.2，build 610b8d0 | 来自docker -v，不据此声称已通过docker version核验服务端API/运行状态 |

## 2. 初次预检历史（不是当前阻断清单）

2026-09-10本机执行[预检工具](../tools/mysql-preflight.mjs)，仅从本地配置读取目标，不打印主机、用户名、密码或数据库名。

- 配置JSON解析成功，必需字段已填写；配置要求TLS和证书校验，未配置专用CA路径。
- 账号名称匹配常见管理员名称；尚未登录，未读取GRANT，不能把名称检查当成权限审计。遵循已约定专用只读账号的准入要求，本轮未使用该凭证尝试认证。
- 单次TCP连接收到MySQL协议10握手；提取数值版本8.1.0；服务端握手声明支持TLS。
- **没有发送账号、密码或任何应用层客户端字节，没有TLS协商，没有SQL。** 握手版本未经认证，不等同于执行SELECT VERSION()；TLS能力标志也不等于证书验证通过。
- 没有查询STORY、DEFECT、历史表或业务行，不得把“网络可达”写成“数据库连接认证成功”。

工具只依赖Node内置模块；限制连接10秒以内、握手缓冲4KiB，输出固定错误类别，不输出服务器原始banner、握手盐或原始异常。原始连接文件保持不变；本地脱敏预检结果位于被忽略的work/。

## 3. 更新后的阻断与后续动作

1. 用户已明确授权本次测试库调查使用现有账号，无需另建专用账号。正式接入仍遵循最小权限；本次保持只读SQL和不采样业务行，不因管理员账号扩展到写操作。
2. 用户启动Desktop后，本地Docker client/server 27.5.1实测可用；已找到并固定缓存MySQL 8.0.43客户端镜像。无需安装mysql2或拉取镜像；未修改现有用户容器。
3. 已收集mysql2@3.23.3的公开npm元数据作为候选，包元数据声明MIT；版本和发行物完整性摘要见[候选记录](research/mysql-driver-candidate-2026-09-10.json)。**未批准、未安装，传递依赖、发行物LICENSE/NOTICE和漏洞尚未完成审查。** 用户确认允许引入后，先完成这些准入和锁文件，再安装；此版本不是“已验证最佳/最新版本”的声明。
4. 已用固定缓存客户端尝试VERIFY_IDENTITY连接，实际错误2026，脱敏类别CERTIFICATE_TRUST_FAILED。配置未指定CA，镜像系统CA无法完成信任验证；尚未确定是否自签或缺少中间证书。下一步补充可信CA并核查主机名，不静默关闭校验。
5. [只读发现工具](../tools/mysql-discover.mjs)已经实现固定元数据SQL的执行包装。TLS阶段失败，SQL没有在真实源库执行；恢复连接后再检查STORY/DEFECT及历史候选表。

用户授权“所有库”覆盖此前连接文件中空schema范围的待确认状态。本轮不修改用户配置；后续查询工具必须显式记录此授权，不将空列表普遍解释成无限范围。

## 4. 元数据SQL范围

首轮查询版本、可见schema、STORY/DEFECT所在schema及按名称匹配的历史候选表。每组最多200行，达到上限后记录结果不完整，使用稳定分页继续，不自动放大超时。

列/索引/约束跟进以查到的schema和表名作绑定值，使用固定information_schema查询。不执行SELECT *、全库COUNT(*)、EXPLAIN ANALYZE、任意过程、DDL或DML。表名未匹配时，再依据受控范围内的列名/注释逐步扩展调查；名称匹配本身不证明历史语义。

原始结构、库名、注释和任何采样结果默认只存本地work/。入Git仅放通用工具、合成测试、脱敏结论；真实字段映射按约定脱敏/确认后提交。

## 5. Compose选择与验证方案

用户已选定 `/root/.docker/cli-plugins/docker-compose` v2.40.3作为NAS唯一部署入口，不再以PATH的v2.20.1为首选。实际部署身份仍需核查，不自动获取或使用root凭据，也不修改全局PATH、软链接、插件或现有容器。

Compose版本选择已由用户确认，尚未验证任何Compose部署文件；如后续发现必需功能或安全问题需要升级，先提出理由并选择同一入口统一部署。Engine 24.0.2与两种Compose的实际服务端API连接、配置和镜像能力均需测试，版本数字不能代替兼容性验证。

部署前流程：

1. 由用户或获授权的操作方式核查选定入口的version、docker version服务端信息、资源和存储；无需把用户加入高权限组或给root口令。
2. 对锁定依赖和镜像生成Compose文件，仅使用所选目标验证过的字段。
3. 使用同一入口执行config --quiet验证解析；避免输出展开的配置，因为可能包含环境变量里的密钥。
4. 经确认在专用项目名、专用卷目录及端口执行启动，验证健康检查、数据库持久化、日志脱敏和停止/恢复。
5. 同一项目的up、ps、logs、stop等管理统一使用选定入口；不使用不明确PATH导致两个版本交替管理。

不得直接在用户NAS执行示例命令或猜测项目目录。存储类型、空闲空间/内存、Container Manager套件信息和实际部署身份仍待核查。

## 6. 初次预检检查与未验证项（后续见14号文档）

已执行：Node预检6项单元/本地假服务测试（配置脱敏、协议解析、畸形包、无效目标、分片握手且无客户端字节、超时）；一次目标无认证握手探测。文档检查、语法及差异检查另在本次提交前执行并报告。

未执行：认证、权限审计、真实SQL、真实表结构发现、驱动安装、NAS远程检查、Compose配置验证/启动及性能测试。工具并非正式数据接入器，不包含自研MySQL认证实现。

官方参考链接（仅协议/工具参考，不包含内部信息；本轮浏览工具未返回可用正文，不宣称本轮已成功复核这些页面）：

- MySQL协议握手：https://dev.mysql.com/doc/dev/mysql-server/latest/page_protocol_connection_phase_packets_protocol_handshake_v10.html
- MySQL信息模式：https://dev.mysql.com/doc/refman/8.0/en/information-schema.html
- Compose配置检查：https://docs.docker.com/reference/cli/docker/compose/config/

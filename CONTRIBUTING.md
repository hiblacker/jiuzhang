# 开发与 Git 协作规范

## 提交原则

- 每完成一个可验证的工作包/文档阶段及时提交，不把一周的变更混入一个提交。
- 推荐类型：`docs:`、`feat:`、`fix:`、`test:`、`chore:`；提交信息说明目的。
- 提交前检查 `git diff --check`、相关测试、文档链接及敏感信息。
- 业务SQL/规则调整同一提交包含口径文档、黄金样本和预期结果。
- 未经授权不重写公共历史、不强制推送、不删除他人未提交变更。
- 仅在真实达到里程碑后创建对应发布标签，不把文档完成标为软件上线。

## 分支与交付

`main`保留经验证基线；实现使用短期功能分支和评审。初始文档可直接在main建立基线。远程仓库由负责人指定；创建本地Git仓库不等于创建或推送GitHub远程仓库。

## 资料与安全

不提交真实业务数据、密码、token、私钥和环境文件。示例使用合成/脱敏数据。证据快照允许保存公开GitHub元数据，但不得保存认证请求头。

尚未指定本项目软件许可证；引入第三方源码/二进制前登记版本、来源、LICENSE/NOTICE和使用方式，勿自动给未来项目代码添加未经确认的开源许可证。

## 建议后续代码布局（尚未创建实现）

- `apps/console`：管理界面。
- `apps/control-api`：平台控制面。
- `adapters`：接入/编排/存储适配。
- `domains`：业务主题模型、SQL、指标定义。
- `contracts`：数据集与接口契约。
- `migrations`：元数据库迁移。
- `tests`：单元、集成、数据样本和端到端测试。
- `deploy`：经验证的环境配置。

不提前创建空工程或引入未决技术栈。

# 开发与 Git 协作规范

详细规则见[工程治理规范](docs/09-engineering-governance.md)。AI先读取[AGENTS.md](AGENTS.md)。

## 当前状态

本地Git仓库已建立，尚无远程仓库、PR规则、分支保护和自动CI。本文是要求，不是已配置状态。文档基线初始化阶段可经本地检查合并；正式多人协作前须配置远程保护和审批。

## 分支策略

- `main`：经验证的集成基线；生产由不可变标签、Git SHA和镜像digest确定，不追踪移动main直接上线。
- `feature/<issue>-<name>`、`fix/<issue>-<name>`、`docs/<topic>`：短期分支，一个工作包一个PR。
- `poc/<topic>`：试验代码和证据；不自动作为正式实现，不默认删除。
- `release/<version>`：可选稳定化分支；没有并行维护需求时不创建。
- `hotfix/<issue>-<name>`：从受影响生产标签或维护线创建，修复后回合并main及相关发布线。
- 不设永久dev/test/uat环境分支；同一制品通过配置晋级不同环境。

远程建立后保护main：禁止强推和删除、至少一名评审、CI/安全检查通过。AI不能代替人工审批。单人本地工作完成检查后可按授权合并，但须明确没有同行评审。

## 提交与PR

每完成一个可验证切片及时提交，类型使用 `docs:` / `feat:` / `fix:` / `test:` / `chore:`。避免积攒一周的混合修改，也不要为及时而提交未经检查的变更到main。

提交前：检查 `git status`、`git diff --check`、相关测试、文档链接、依赖和敏感信息；暂存后再次执行 `git diff --cached --check`。失败停止提交，不忽略退出码。

PR描述包括：目标/Issue、范围、接口/数据兼容性、测试证据、风险、回滚/前向修复、历史重算影响。可注明AI参与范围及人工验证方式；不得伪造评审人或测试结果。

业务SQL/指标变化与口径文档、黄金样本、预期结果同一PR。生产不得只在控制台改SQL而不回写Git。

## 资料、安全与许可证

不提交生产数据、密码、token、密钥、环境文件、临时下载包。合成样例和公开研究证据可以提交。未指定本项目自身软件许可证，不自动将用户项目开源。

第三方组件固定版本，登记来源、许可证/NOTICE、传递依赖和镜像digest；使用含有限制条款的代码先审批，不能把商用等同于无限制托管/分发。

## 推荐代码布局（尚未创建实现）

- `apps/console`：Vue/TypeScript控制台。
- `apps/control-api`：Java/Spring控制面。
- `adapters`：接入、编排和存储适配。
- `domains`：主题SQL、指标和数据测试。
- `contracts`：API和数据集契约。
- `migrations`：不可覆盖的元数据库迁移。
- `tests`：单元、集成、边界和端到端。
- `experiments`：可复现PoC代码、锁定版本和结果，不打入正式应用镜像。
- `deploy`：经验证的Compose或企业平台配置。
- `work`：忽略的临时产物，不保存唯一验收证据。

未授权不强推、不重写公共历史、不删除用户变更；未经请求不创建远程仓库或推送。仅达到真实里程碑后创建版本标签。

# Luna 开发任务：完成整库与持续入湖系统

你是本仓库的开发执行助手。请直接在本地实现、运行、验证并修复下面的系统，不停留在重新写方案或列待办。不要估工时、排周计划或承诺今晚必然全部完成；按可运行成果连续推进。

## 目标与执行顺序

项目是通用数据仓库平台，DevOps 只是首个业务主题。首先让用户提供的整个 MySQL 业务库完成首次原始入湖及每日可靠更新，随后完成每日文件、REST API、建模质量、数据集发布和控制台。全部目标都在范围内，按下列依赖顺序实施，不把先交付一个切片误称为整个任务完成。

1. 整库全量入湖、原始资产、表覆盖和系统批次。
2. 每日数据库运行、幂等、失败恢复、缺失与补采、结构变化。
3. 每日目录文件接收与 CSV、Excel、JSON/JSONL、Parquet 解析。
4. REST API 分页、认证引用、限流、原响应留存与恢复。
5. 版本化 SQL 模型、质量检查、候选构建、数据集发布与授权消费。
6. 随后端切片逐步完成真实可操作的控制台，以及本地部署、备份恢复和操作手册。

## 开始前必须读取

工作目录：`/Users/carson/Documents/code/jiuzhang`。

先检查 `git status` 和当前分支，保留已有修改，然后读取：

- `AGENTS.md`、`CONTRIBUTING.md`、`README.md`。
- `docs/02-architecture.md`、`docs/03-data-processing.md`、`docs/07-decisions-risks.md`、`docs/09-engineering-governance.md`。
- `docs/31-warehouse-implementation-plan.md`：当前执行主线。
- `docs/32-whole-database-discovery.md`：真实来源事实和首批计划。
- `docs/33-lake-implementation-design.md`：对象、提交协议、接口操作、工作包和验收。
- 按实现涉及范围读 `docs/22`–`docs/29`、现有迁移及 Worker 实际代码。

当前优先级与取消工时要求以 31–33 为准。04、21 的旧排期和仅 STORY/DEFECT 接入范围不是当前边界；旧业务规则仅用于对应领域，不能未经验证套到整个源库。

## 已完成事实，直接利用

- 已经实际连接指定源，确认 MySQL 8.1.0、TLSv1.3 和只读会话。
- 最近发现 157 张基础表、2,228 个字段，全为 InnoDB；3 张表无主键，无视图。
- 元数据估计约 158.5 万行、600 MiB；这不是实际 COUNT、抽取量或容量承诺。
- 本地 `work/lake-foundation/inventory.json` 有真实结构；`work/lake-foundation/initial-full-snapshot-plan.json` 已为全部表生成首次 FULL_SNAPSHOT 计划。使用前复核来源与结构变化，不把 157 硬编码到产品。
- **提示词创建时的基线是“真实业务数据尚未抽取、每日任务尚未配置”。** 现有 Java Worker 主要处理合成 JSONL，不能把旧批次接口当成真实接入完成；以下当前实现进展记录已更新。
- **当前实现进展（执行本提示词后更新）：** `tools/lake-ingest.mjs` 已完成真实 MySQL 全量与一个每日窗口；批次 `full-fixed3-20260915` 和每日批次已逐表校验，重复窗口已复用。`tools/file-ingest.mjs`、`tools/rest-ingest.mjs`、`tools/lake-model.mjs`、`tools/lake-daily.mjs`、`tools/lake-register.mjs`、控制 API 的清单/manifest 登记接口和只读控制台已经加入并有测试；不要重复研究或回退已验证链路，直接继续接入真实目录/API 配置、启动本地控制面做登记和恢复演练。
- 既有 Spring Boot 控制 API、来源/任务/批次、租约心跳、重试取消、RAW 清单和 PostgreSQL 角色已有实现，先读代码后复用。

## 测试源授权与敏感信息

- 连接配置只从本地 `secrets/mysql-development.local.json` 读取，不在聊天、提交、日志或页面中输出主机、账号、密码和业务明细。
- 用户已明确允许**这个测试源的调查及入湖使用 TLS 例外**：强制 TLS 加密，不校验证书身份。授权记录为 `secrets/mysql-development-tls-authorization.local.json`，绑定连接配置摘要，正式环境不适用。
- 不再重复询问此测试源 TLS 例外。保持默认严格校验，运行时显式应用已授权来源的例外；如果连接配置发生实质变化，先核验仍是同一授权来源。
- 用户已要求把指定业务库全部入湖；原模板中的 metadata-only 是此前调查阶段设置，不能把它误当成当前用户禁止抽取业务数据。将当前授权转为明确的测试接入配置，保留来源范围、只读约束和审计，不扩大到其他库。
- 源库只读，不执行 DDL/DML、权限更改、自动加索引、启用日志或清理源数据。真实湖数据和结构写入批准的本地受限目录并保持 Git 忽略；合成夹具与测试可以提交。

## 本机环境与国内源

先检查已有 Java、Maven、Node、pnpm、Python、MySQL CLI、Docker 和缓存，优先复用。本机 MySQL CLI 曾实测 9.7.1，Java 21/Maven 和阿里云 Maven 源已用于测试；不要再次断言没有环境，也不要为同一工具另装一套。

- Maven 优先沿用本机已有阿里云镜像设置；不得覆盖用户其他项目的全局配置。
- 前端包使用国内 npm 镜像，例如 `https://registry.npmmirror.com`，采用项目级或单次命令配置并提交锁文件。
- 若确实需要 Python 安装，使用已配置的国内源；没有时可采用清华 PyPI 镜像 `https://pypi.tuna.tsinghua.edu.cn/simple`，精确固定依赖。
- Docker 优先复用已有明确版本镜像；需要拉取时使用用户现有可信国内加速配置，不猜测第三方镜像代理，不修改全局 daemon 设置或导入未知来源镜像。
- 使用 HTTPS，不关闭包下载证书检查。镜像源只改变下载路径，不能省略版本、校验、许可证和供应链检查；连接器、驱动、解析器与运行镜像都必须按仓库规则准入。
- 沿用 Java/Spring Boot、PostgreSQL、Vue 3/TypeScript/Naive UI 和既有 SeaTunnel/DolphinScheduler/dbt 路线。依赖版本读取已有锁定记录；缺少的依赖先锁版本、检查许可证及适配，再按准入规则处理，不使用 latest、动态版本或未批准依赖。

## 不可省略的实现要求

### 整库与每日数据库

全部基础表、空表和无主键表都有明确结果，不静默跳过。先全量建立基线，逐表证明增量条件后再优化；更新时间、自增 ID 都不能单独保证不漏数，硬删除和 CDC 另有契约。

原始数据类型保真，流式分片；Schema、行数、字节数、哈希、来源与实际时间随对象保存。共享快照必须实际证明，多个独立读取连接不能默认当作同一时点。快照失效后重建相应单元，不把新旧数据拼成一个假快照。

先持久化不可变对象，再登记清单与提交状态；全部必需表完成后才切换最新完整批次。覆盖磁盘满、进程退出、元数据提交失败、过期租约和陈旧提交。

每日计划有持久化窗口账本、明确时区、幂等键、attempt/revision、重试和漏跑发现。服务重启能发现遗漏、限制补跑并发；旧完整批次保留。源库只有当前态时，补采只能记录实际时点，不能冒充历史快照。

### 每日文件

外部只负责落目录，不绑定传输工具。明确交付模式和完成信号，多文件包要判齐；区分没有收到、合法空交付、重复、迟到、更正、半写和断连。

原件不覆盖、不自动搬走或删除。文件发现与解析分开；四格式各自有契约及测试，不能把保存文件成功当作解析可用。限制路径与符号链接、文件大小、解析资源和异常行行为。

### API、模型、发布和界面

API 请求受目标白名单、认证引用、分页上界、循环检查、限流和错误契约控制；跨域 next-link 不泄露凭证。先用模拟服务验证可独立完成的通用能力，缺真实 API 不阻塞数据库/文件，但不能声称真实接口已接入。

模型采用版本化 SQL，明确粒度、键、时间、依赖和输入版本；质量通过才发布，旧运行不可覆盖新版本。未知业务规则标明未确认，不编造公式和历史。消费侧强制权限、参数化、分页/限额与 release 一致性。

页面使用真实接口展示来源、表覆盖、批次、历史、异常、资产、模型、质量及发布；先完成整库相关必要页面，随后扩充。任务在独立后台执行，不能依赖页面保持打开。

## 验证、提交与持续推进

在当前已提交基线上创建短功能分支，建议 `codex/lake-foundation`；分支已存在时检查用途，不重置或覆盖。不要启动子代理，我会用 Luna 本身执行。

每个工作包都同时提交实现、测试、接口/数据契约、迁移和恢复说明。运行相关单元、集成及真实链路测试；文档执行 `node tools/check-docs.mjs`，提交前执行 `git diff --check` 并检查暂存敏感内容。新增运行时行为不能只用搜索源码字符串的测试验证。

优先完成本地可运行系统，真实源只读联调。使用隔离资源和明确版本，不覆盖用户既有容器、数据卷或配置。NAS/生产部署、数据库清理、远程推送或发布标签仍需用户明确指令；不要把这些边界当作停止本地开发的理由。

完成一个切片后继续下一个，遇到失败先定位并修复，不反复问是否继续。只有缺少不可替代的凭据、业务定义、来源样例、外部访问或确需新增授权时提出具体问题，并继续不依赖它的工作。不得为避免询问而绕过安全约束或虚构结果。

进度说明简短报告已经运行的结果、当前问题和正在完成的内容。任务若跨上下文，更新本地进度文件，记录已完成、准确命令、提交、失败及下一入口；压缩后从该状态继续，不重复研究或重做已经验证的步骤。

最终交付：本地可启动代码与界面、部署配置、版本迁移、整库实际入湖摘要、每日与文件/API验收、模型与发布验证、操作及恢复手册、提交编号和明确未完成项。分别说明合成测试、真实源、实际跨日、本地部署和 NAS/生产的验证状态；不能用“计划支持”充当“已完成”。

现在从检查工作区、读取 31–33 和已有 Worker 开始，直接执行 LAKE-01，完成后继续 LAKE-02 及其余工作包。
